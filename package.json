{
  "name": "modl",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "tsc --build",
    "verify": "npm run typecheck && npm test && npm run test:e2e",
    "test:e2e": "npm run test:e2e --workspace @modl/app",
    "dev": "npm run dev --workspace @modl/app",
    "build": "npm run build --workspace @modl/app"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
